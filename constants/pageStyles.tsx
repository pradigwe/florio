import { StyleSheet } from "react-native";

export const pageStyles = StyleSheet.create({
  default: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "#9db39eff",
  },
});
